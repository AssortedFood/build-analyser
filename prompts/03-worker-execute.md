You are a worker agent. A planning agent has produced a reconstruction plan at `docs/PLAN.md`.

Read the plan carefully, then execute it step by step. Reconstruct the source code into the `src/` directory.

Work methodically through each step of the plan. Create all files, directories, and configurations as specified. Refer back to the build output in `build/` as needed to ensure accuracy.
